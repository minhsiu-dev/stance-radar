# Opt-in override: route the api's scraping (transcripts + yfinance) through a
# rotating NordVPN exit via gluetun. Bring up with:
#   docker compose -f docker-compose.yml -f docker-compose.vpn.yml up -d
# Requires OPENVPN_USER / OPENVPN_PASSWORD / SERVER_COUNTRIES in .env (see .env.example).
services:
  gluetun:
    image: qmcgaw/gluetun:v3.40.0
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    environment:
      VPN_SERVICE_PROVIDER: nordvpn
      VPN_TYPE: openvpn
      OPENVPN_USER: ${OPENVPN_USER}
      OPENVPN_PASSWORD: ${OPENVPN_PASSWORD}
      SERVER_COUNTRIES: ${SERVER_COUNTRIES:-United States}
      HTTPPROXY: "on"
      HTTPPROXY_LISTENING_ADDRESS: ":8888"
      # control server defaults to :8000 (unauthenticated) — used by ProxyRotator

  api:
    environment:
      FETCH_PROXY_URL: http://gluetun:8888
      GLUETUN_CONTROL_URL: http://gluetun:8000
    depends_on:
      gluetun:
        condition: service_healthy

  # Transcript fetching (YouTubeTranscriptApiClient) runs in the worker container, not
  # api -- without this block it would fetch direct and unproxied, silently bypassing
  # the VPN for exactly the traffic most likely to get IP-blocked. yfinance stays in
  # api, so leaving this off only half-breaks the setup, which is easy to miss.
  worker:
    environment:
      FETCH_PROXY_URL: http://gluetun:8888
      GLUETUN_CONTROL_URL: http://gluetun:8000
    depends_on:
      gluetun:
        condition: service_healthy
