# 必填
YOUTUBE_API_KEY=your-youtube-data-api-key

# 選填(預設值如下)
# LLM 透過 Claude Code CLI (`claude -p`) 跑;本機需先 `claude login` 過,
# 並讓 ~/.claude 掛進 api 容器。不需 ANTHROPIC_API_KEY。
CLAUDE_BIN=claude
CLAUDE_MODEL=claude-haiku-4-5
BACKFILL_LIMIT=30
ANALYSIS_CONCURRENCY=4
