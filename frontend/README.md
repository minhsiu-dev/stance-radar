# Stance Radar — frontend

The Next.js frontend for Stance Radar. See the [root README](../README.md) for
setup and usage. In normal use it runs as part of the three-container stack via
`docker compose` from the repo root, not standalone.
